# Slim redesign skeleton — most v1 secrets are no longer required.
# Prefix form still works via pydantic-settings (MDPREFLIGHT_*).

# Optional: override CORS (comma-separated not supported; use code defaults or env JSON if extended later)
# MDPREFLIGHT_APP_NAME=MD Preflight Pipeline
